generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../data/wiki-tracer-ts.db"
}

model User {
  id      Int      @id @default(autoincrement())
  email   String   @unique
  name    String?
}
