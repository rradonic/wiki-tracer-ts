export const BATCH_SIZE = 5000;

// max number of batches to load, useful for debugging
export const PAGE_LOAD_LIMIT = Number.MAX_SAFE_INTEGER;
export const LINK_LOAD_LIMIT = Number.MAX_SAFE_INTEGER;
